def boundary(array):
    """array should be a 2D numpy array with entries of type bool.
    returns a path, meaning a list of pairs of floats, tracing the boundary of the True region
    with a distance of 1 between subsequent points."""